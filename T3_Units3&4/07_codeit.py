# Define a function which takes two string parameters
# - It should return True if the second string parameter can be found inside the first string parameter
#   = For example, if the words "program" and "pro" were used, the function would return True
#   = If the words "coding" and "coder" were used, the function would return False
# Call this function twice using the two examples above to test your function
