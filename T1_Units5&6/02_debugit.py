# Fix the errors in this program
# - The program generates a random integer between 0 and 100
# - If this random integer is less than 40, it outputs "Good luck on the next attempt"
# - Otherwise, if this random integer is less than 80, it outputs "Good job on passing that test"
# - Otherwise, it outputs "Amazing work on that test!"
import random


score = random.randint(0, 100)
if ?:
    print("Good luck on the next attempt!")
elif ?:
    print("Good job on passing that test")
else ?:
    print("Amazing work on that test!")
