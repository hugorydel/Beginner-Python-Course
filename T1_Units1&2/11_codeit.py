# Create a program that makes three variables
# It then outputs the double and half of each variable
# Output them in three lists with the variable before the list, and it's double and half values as two items in the tab formatted list
# For example
# Number: 20
#   Double = 40
#   Half = 10
