# Swap the 'a' and 'b' variables around
# So the value in 'a' should go into 'b'
# And the value in 'b' should go into 'a'
# HINT: use the 'temp' variable to store one value before moving the other value across
a = 12
b = 4
temp = 0

print(f"Before Swap: a = {a}, b = {b}")


print(f"After Swap: a = {a}, b = {b}")
