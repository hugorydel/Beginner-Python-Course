# Create a program that makes three variables for three different ages
# It then outputs the average of those ages
# Averages can be calculated by adding all the numbers together
# And then dividing that sum by the number of values that were added
