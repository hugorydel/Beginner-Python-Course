# Create a program that generates a random integer (0 to 10 inclusive)
# - It should repeatedly ask the user to guess what the number is
#   = Handle any possible errors that might cause the program to crash when getting the user's input
# - Check if the user's guess is correct
#   = If so, end the while loop
# - Otherwise, output whether the user's guess was too low or too high
