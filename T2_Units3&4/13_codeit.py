# Create a program that makes an empty list of numbers
# - Repeatedly ask the user if they want to add another number to the list
#   = If they do, get a number from the user and add it to the list
#   = Otherwise, end the loop
# - Loop through the numbers after the user finishes adding to them and calculate the average value
#   = Add up all the numbers to get a total
#   = Then divide by the number of elements in the list
#       == Can retrieve the number of elements in a list using the len(list_name) function
