# Fix the errors in this program
# - It should create a list of numbers (1 to 5)
# - It should then use a while loop to double each number in the list
# - It then prints the doubled list
numbers = [1, 2, 3, 4, 5]
index = 0
for index < len(numbers):
    numbers[index] *= 2
    index += 1
print(numbers)
