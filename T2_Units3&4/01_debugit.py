# Fix the errors in this program
# - It should loop through the list of numbers
# - For each number, it outputs the double of that number
numbers = [1, 4, 9, 16]
for number in numbers:
    print(numbers * 2)
