# Edit this program by removing the "baker" and "carpenter" jobs from the list before the for loop
# - Use functions to do this (do not just remove them from the list)
jobs = ["actor", "baker", "builder", "programmer", "carpenter"]
for job in jobs:
    print(job)
